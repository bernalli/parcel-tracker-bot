# Copy to `docker-compose.override.yml` (gitignored) for local customizations.
# Example: mount plugins/ in rw mode for development, expose the metrics port, etc.

services:
  parcel-tracker:
    volumes:
      # Local rw plugin overlay (for developing Italian carrier plugins)
      - ./plugins:/app/plugins:rw
    # ports:
    #   - "9090:9090"  # Prometheus metrics endpoint
